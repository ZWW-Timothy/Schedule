name: 'reminder'

on:
#  push:
  schedule:
    - cron: '51 7 * * *'
    - cron: '51 15 * * *'

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v1
      - name: 'Run reminder'
        run: bash ./reminder.sh
