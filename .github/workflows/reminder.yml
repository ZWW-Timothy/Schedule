name: 'reminder'

on:
#  push:
  schedule:
    - cron: '55 7 * * *'
    - cron: '3 8 * * *'
    - cron: '51 15 * * *'

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v1
      - name: 'Run reminder'
        run: bash ./reminder.sh
